package test

import (
	"testing"
)

func TestRepository(t *testing.T) {
	/*t.Run("TestRepository", func(t *testing.T) {
		r := repository.New()
		listShapes, err := r.ListShapesByType("RECTANGLE")

		assert.NoError(t, err)
		assert.NotEmpty(t, listShapes, "Lista no debería estar vacía.")
	})

	t.Run("TestRepository empty", func(t *testing.T) {
		r := repository.New()
		listShapes, err := r.ListShapesByType("AA")

		assert.NoError(t, err)
		assert.Empty(t, listShapes, "La lista debería estar vacía.")
	})*/
}
